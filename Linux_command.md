查看某进程：
```ps -p [PID]```

查看其开始时间：
```ps -p [PID] -o lstart```

解压zip：
```unzip [file.zip]```

ssh:
连接：
```ssh -p [port_number] [username]@[server_ip]```

docker:
改名：
```docker rename [CURRENT_NAME] [NEW_NAME]```
kill:
```docker kill [container_name]```